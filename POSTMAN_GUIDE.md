# دليل استخدام Postman Collection

## 📦 الملفات المتوفرة

1. **`Rainz_Auth_API.postman_collection.json`** - Collection كامل لجميع الـ APIs
2. **`Rainz_Auth_Development.postman_environment.json`** - Environment للتطوير

---

## 🚀 كيفية الاستيراد في Postman

### 1. استيراد Collection

1. افتح Postman
2. اضغط على **Import** في الزاوية العلوية اليسرى
3. اسحب ملف `Rainz_Auth_API.postman_collection.json` أو اضغط **Choose Files**
4. اضغط **Import**

### 2. استيراد Environment

1. اضغط على **Import** مرة أخرى
2. اسحب ملف `Rainz_Auth_Development.postman_environment.json`
3. اضغط **Import**
4. من القائمة العلوية، اختر **Rainz Auth - Development** كـ Environment نشط

---

## ⚙️ إعداد Environment

بعد الاستيراد، قم بتحديث المتغيرات التالية حسب بيئتك:

### المتغيرات الأساسية

| المتغير | القيمة الافتراضية | الوصف |
|---------|-------------------|-------|
| `base_url` | `http://localhost` | عنوان الـ API الخاص بك |
| `device_uuid` | `550e8400-...` | معرف الجهاز للاختبار |
| `device_token` | `fcm_token_...` | FCM token للاختبار |
| `test_email` | `test@example.com` | بريد إلكتروني للاختبار |
| `test_mobile` | `0512345678` | رقم هاتف للاختبار |
| `test_password` | `Password@123` | كلمة مرور للاختبار |

### المتغيرات التلقائية (لا تحتاج تعديل)

| المتغير | الوصف |
|---------|-------|
| `access_token` | يتم حفظه تلقائياً بعد Login/Register |
| `user_id` | يتم حفظه تلقائياً بعد Login/Register |

---

## 📋 الـ Requests المتوفرة

### ✅ Requests الجديدة

1. **Register (NEW)** - تسجيل حساب جديد
   - يحفظ الـ token تلقائياً
   
7. **Change Password (NEW)** - تغيير كلمة المرور
   - يتطلب كلمة المرور الحالية

### ✅ Requests المعدلة

2. **Login with Email (UPDATED)** - تسجيل دخول بالبريد
   - تغيير: `login` بدلاً من `mobile`
   - تغيير: `password` بدلاً من OTP
   
3. **Login with Mobile (UPDATED)** - تسجيل دخول بالهاتف
   - نفس الـ endpoint، login يقبل mobile
   
5. **Update Profile - Name & Email (UPDATED)** - تحديث الاسم والبريد
   - جديد: دعم تحديث email
   - حذف: نظام OTP
   
6. **Update Profile - Mobile (UPDATED)** - تحديث الهاتف
   - حذف: نظام OTP للتأكيد

### ✅ Requests بدون تغيير

4. **Get Profile** - عرض الملف الشخصي
8. **Store Name** - تخزين الاسم
9. **Logout** - تسجيل الخروج
10. **Delete Account** - حذف الحساب

### ❌ Requests المحذوفة

- **Activate OTP** - تأكيد OTP
- **Resend OTP Code** - إعادة إرسال OTP
- **Confirm Mobile Change** - تأكيد رقم الهاتف الجديد

> **ملاحظة:** الـ requests المحذوفة موجودة في مجلد منفصل للمرجعية فقط

---

## 🔄 دورة الاختبار الكاملة (Test Cycle)

### السيناريو 1: تسجيل مستخدم جديد

```
1. Register (NEW) ✅
   ↓ (يحفظ token تلقائياً)
2. Get Profile
   ↓
3. Update Profile - Name & Email (UPDATED)
   ↓
4. Change Password (NEW)
   ↓
5. Logout
```

### السيناريو 2: تسجيل دخول مستخدم موجود

```
1. Login with Email (UPDATED) ✅
   ↓ (يحفظ token تلقائياً)
2. Get Profile
   ↓
3. Update Profile - Mobile (UPDATED)
   ↓
4. Logout
```

### السيناريو 3: تسجيل دخول بالهاتف

```
1. Login with Mobile (UPDATED) ✅
   ↓ (يحفظ token تلقائياً)
2. Get Profile
   ↓
3. Change Password (NEW)
   ↓
4. Login with Email (للتأكد من تغيير كلمة المرور)
```

---

## 🔐 ملاحظات الأمان

### كلمة المرور

يجب أن تحتوي على:
- ✅ 8 أحرف على الأقل
- ✅ أحرف كبيرة (A-Z)
- ✅ أحرف صغيرة (a-z)
- ✅ أرقام (0-9)
- ✅ رموز خاصة (!@#$%^&*)

**أمثلة صحيحة:**
- `Password@123`
- `MyPass@2026`
- `Secure#Pass1`

**أمثلة خاطئة:**
- `password` (لا يوجد أحرف كبيرة، أرقام، رموز)
- `PASSWORD123` (لا يوجد أحرف صغيرة، رموز)
- `Pass@1` (أقل من 8 أحرف)

### رقم الهاتف

- ✅ يبدأ بـ `05`
- ✅ يتبعه 8 أرقام
- ✅ مثال: `0512345678`

---

## 📊 رموز الاستجابة (Response Codes)

| الكود | المعنى | متى يحدث |
|------|--------|----------|
| 200 | نجاح | طلب ناجح (Login, Get Profile, Update) |
| 201 | تم الإنشاء | تسجيل ناجح (Register) |
| 401 | غير مصرح | بيانات دخول خاطئة |
| 403 | محظور | الحساب محظور |
| 422 | خطأ في البيانات | validation فشل |

---

## 🧪 أمثلة الاستجابات

### تسجيل ناجح (201)
```json
{
  "status": true,
  "message": "تم إنشاء الحساب بنجاح",
  "data": {
    "user": {
      "id": 1,
      "name": "أحمد محمد",
      "email": "ahmed@example.com",
      "mobile": "0512345678"
    },
    "access_token": "1|abcd..."
  }
}
```

### خطأ في البيانات (422)
```json
{
  "status": false,
  "message": "The email has already been taken.",
  "errors": {
    "email": [
      "البريد الإلكتروني مُستخدم من قبل."
    ]
  }
}
```

### بيانات دخول خاطئة (401)
```json
{
  "status": false,
  "message": "البريد الإلكتروني/رقم الهاتف أو كلمة المرور غير صحيحة"
}
```

---

## 🎯 نصائح الاستخدام

### 1. استخدام المتغيرات

بدلاً من كتابة القيم يدوياً، استخدم المتغيرات:

```json
{
  "email": "{{test_email}}",
  "password": "{{test_password}}"
}
```

### 2. حفظ Token تلقائياً

الـ Collection يحفظ الـ token تلقائياً بعد Login/Register في:
- `{{access_token}}`
- `{{user_id}}`

### 3. تغيير اللغة

في الـ Headers، غير قيمة `Accept-Language`:
- `ar` للعربية
- `en` للإنجليزية

### 4. اختبار Validation

جرب إرسال بيانات خاطئة لاختبار رسائل الخطأ:
- كلمة مرور ضعيفة
- بريد إلكتروني مكرر
- رقم هاتف خاطئ

---

## 🔄 التحديثات من النظام القديم

### ما تم حذفه ❌

- نظام OTP بالكامل
- endpoint `/activate`
- endpoint `/resend-code`
- endpoint `/confirm-new-mobile`

### ما تم إضافته ✅

- endpoint `/register`
- تغيير كلمة المرور في `/edite-profile`
- تسجيل دخول بـ email/mobile + password

### ما تم تعديله 🔄

- `/login` - يقبل email/mobile + password
- `/edite-profile` - يدعم email و password

---

## 📞 الدعم

إذا واجهت مشاكل:
1. تأكد من تشغيل الـ server
2. تأكد من اختيار الـ Environment الصحيح
3. تأكد من صحة الـ `base_url`
4. راجع ملف `AUTH_SYSTEM_UPDATES.md` للتفاصيل

---

**آخر تحديث:** 2026-02-02
**الإصدار:** 2.0 (Email/Mobile + Password)
